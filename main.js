let numeroTabellina = Number(prompt('inserisci il numero per la quale vorresti vedere la tabellina'));
for (let i = 0; i < 11; i++) {
    console.log(`Per il numero ${numeroTabellina}, ${numeroTabellina}*${i} è uguale a ${numeroTabellina*i}`);  
}